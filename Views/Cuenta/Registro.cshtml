@model TiendaPc.Controllers.RegistroVm
@{
  ViewData["Title"] = "Crear cuenta";
  Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="max-w-md mx-auto bg-white rounded-xl shadow p-6">
  <h1 class="text-2xl font-semibold mb-4">Crear cuenta</h1>
  <form method="post" action="/Cuenta/Registro">
    @Html.AntiForgeryToken()
    <input type="hidden" name="ReturnUrl" value="@Model.ReturnUrl" />
    <div class="mb-3">
      <label class="block text-sm font-medium">Email</label>
      <input asp-for="Email" class="w-full border rounded px-3 py-2" />
      <span asp-validation-for="Email" class="text-red-600 text-sm"></span>
    </div>
    <div class="mb-3">
      <label class="block text-sm font-medium">Contraseña</label>
      <input asp-for="Password" class="w-full border rounded px-3 py-2" />
      <span asp-validation-for="Password" class="text-red-600 text-sm"></span>
    </div>
    <div class="mb-4">
      <label class="block text-sm font-medium">Confirmar contraseña</label>
      <input asp-for="ConfirmPassword" class="w-full border rounded px-3 py-2" />
      <span asp-validation-for="ConfirmPassword" class="text-red-600 text-sm"></span>
    </div>
    @if(!ViewData.ModelState.IsValid)
    {
      <div class="text-red-600 text-sm mb-2">
        @foreach (var e in ViewData.ModelState.Values.SelectMany(v=>v.Errors)) { <div>@e.ErrorMessage</div> }
      </div>
    }
    <button class="w-full px-4 py-2 rounded bg-black text-white">Crear cuenta</button>
  </form>
  <div class="text-sm text-center mt-3">
    ¿Ya tienes cuenta? <a class="underline" href="/Cuenta/Login@(string.IsNullOrWhiteSpace(Model.ReturnUrl) ? "" : $"?returnUrl={Uri.EscapeDataString(Model.ReturnUrl!)}")">Ingresar</a>
  </div>
</div>

@section Scripts{
  <partial name="_ValidationScriptsPartial" />
}
